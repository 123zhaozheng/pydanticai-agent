# Pydantic Deep - Environment Configuration
# 这是一个环境变量配置文件模板。
# 复用此文件：复制为 .env (Git 会忽略该文件)

# [可选] 项目根目录覆盖
# 如果不设置，默认会自动计算为当前代码所在的根目录
# 设置此变量可以强制指定项目的根路径，例如：
# PYDANTIC_DEEP_BASE_DIR=D:/code/py/pydanticai-agent

# 其他潜在配置 (未来扩展)
# LOG_LEVEL=INFO
# DATABASE_URL=sqlite:///app.db
